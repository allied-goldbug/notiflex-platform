apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: notiflex-secrets
  namespace: notiflex
spec:
  provider: gke
  parameters:
    secrets: |
      - resourceName: "projects/project-d64f9b5c-20c8-4906-95b/secrets/valkey-password/versions/latest"
        path: "valkey-password"
